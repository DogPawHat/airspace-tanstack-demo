[build]
  command = "vite build"
  publish = "dist/client"

[dev]
  command = "vite dev"
  port = 3000

[build.environment]
  # Set these in the Netlify UI instead (they're secrets):
  # AIRSPACE_SERVICE, AIRSPACE_PDS_INVITE_CODE, AIRSPACE_SESSION_SECRET
  NODE_VERSION = "22"
