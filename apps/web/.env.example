# the demo PDS the sandbox accounts live on
AIRSPACE_SERVICE=http://localhost:2583
# `pnpm pds:invite` prints a code; the local PDS requires invites
AIRSPACE_INVITE_CODE=
# 32+ chars, signs the demo-account session cookie
SESSION_PASSWORD=
# fallback service account, only used by `npx tsx seed.ts`
AIRSPACE_IDENTIFIER=alice.test
AIRSPACE_PASSWORD=hunter2
