{
  "name": "airspace-tanstack-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.4.2",
      "onFail": "download"
    }
  },
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "typecheck": "pnpm -r typecheck",
    "pds": "pnpm --filter airspace-demo-pds dev",
    "pds:invite": "pnpm --filter airspace-demo-pds invite",
    "pds:smoke": "pnpm --filter airspace-demo-pds smoke",
    "pds:reset": "pnpm --filter airspace-demo-pds reset"
  }
}
